# 2026-04-09 工作日志

## 品牌名称更新
- 将网站所有文件中的"巨骏新材料"统一更新为**广俊新材料**
- 更新了：index.html（标题、导航Logo、公司介绍、页脚、版权）、DESIGN.md、所有CSS和JS注释
- Logo图标从"巨"改为"广"
- 邮箱域名更新为 sales@guangjun-materials.com
## 品牌Logo集成
- 用户提供了广俊新材料的六边形蓝橙色Logo（类似字母G的设计）
- 创建了SVG矢量版Logo（assets/images/logo.svg），替换导航和页脚中的文字Logo
- 导航和页脚现在都显示品牌图片+文字组合
## 全站图片本地化
- 下载了19张高质量图片到 assets/images/ 目录
- 将HTML中所有Unsplash外链替换为本地文件路径
- 涵盖：Hero、关于我们(2)、产品中心(5)、应用领域(4)、成功案例(3)、技术实力(1)、新闻动态(3)
- 页面不再依赖外部链接，可离线正常显示
## 产品中心横向滚动改造
- 产品中心从 3列 Grid + 特色大卡片 改为 横向滚动等宽卡片布局（flex:0 0 300px）
- 新增 products-scroll-wrapper/products-scroll 结构，带左右渐变遮罩
- 新增 "左右滑动查看更多产品" 提示条
- 解决了内容超出 100vh 屏幕的问题

## 资质认证页面重新设计
- 原设计：6个相同盾牌图标的小卡片排成一行，显得空荡荡单薄
- 新设计：左右分栏布局
  - 左侧：品质承诺视觉区（旋转光晕动画大徽章 + 数据指标 + 质量标签）
  - 右侧：6张认证详情卡片（2列网格，每个有不同专属图标和配色）
- HTML 和 CSS 完全重构（cert-layout, cert-hero, cert-cards-grid 等）

## 紧急修复：Git合并冲突导致页面崩溃（第二轮）
- **index.html** — `<head>` 部分 git 冲突（两套不同 meta/title/fonts/styles 导致CSS全部丢失）+ `</body>` 前的 script 冲突 → 完全重写整个 index.html
- **base.css** — 文件开头的 git 冲突标记导致 CSS 变量声明被破坏 → 清除冲突恢复完整注释头
- **上一轮已修复**: app.js / responsive.css / sections.css

## 第一轮紧急修复：Git合并冲突导致页面崩溃
- **app.js** — 6处 git 合并冲突标记 + 混入其他项目的粒子动画/暗色主题代码 → 完全重写恢复正确代码
- **responsive.css** — 整个文件被替换成了另一个项目的不相关代码（.prod-hero, .cap-core等不存在的类名）→ 完全重写为匹配当前HTML结构的响应式规则
- **sections.css** — 清理了上一轮残留的冲突标记和约60行无关代码

## 全屏一屏一屏布局改造
- 将所有 .section 模块改为 min-height: 100vh + flex 垂直居中布局
- Hero 区域设为精确 100vh 全屏（height: 100vh）
- CTA 区域也改为 100vh 全屏居中
- 精简了各模块的间距和字体大小以适配一屏展示：
  - About/Products/Applications/Cases/Tech/Certifications/News/Contact 全部优化
  - 产品卡片、案例卡片、新闻卡片间距收紧
  - 技术设备列表、联系表单更紧凑
- 添加了右侧侧边导航指示器（side-nav），10个圆点对应各模块
  - 滚动时自动高亮当前所在模块
  - hover 显示模块名称
  - 移动端自动隐藏
- 移动端响应式：取消 100vh 限制，恢复自然流式布局
- JS 新增 initSideNav() 函数处理侧边导航滚动激活状态
